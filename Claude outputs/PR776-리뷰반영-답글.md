리뷰 감사합니다. 지적하신 두 가지 반영해서 `6fdfde6`으로 올렸습니다.

## 1. `test-contract` FAIL (blocking)

원인은 `scripts/deployment.sh`의 `unset` 순서였습니다. `tests/contract/test_signup_production_build.py`가 아래 블록이 **연속**임을 assertion하는데, 제가 그 사이에 `unset VITE_PUBLIC_TRACK_C`를 끼워 넣어 깨졌습니다.

```python
assert 'unset VITE_SIGNUP_TERMS_APPROVED\nset -a\nsource "$PROD_ENV_FILE"' in script
```

두 `unset`의 순서만 바꿔 계약을 복원했습니다. 동작은 동일합니다.

```bash
unset VITE_PUBLIC_TRACK_C
unset VITE_SIGNUP_TERMS_APPROVED
set -a
source "$PROD_ENV_FILE"
```

`test_terms_configuration_requires_explicit_boolean` 7개 파라미터 전부 통과합니다.

덧붙여 같은 회귀가 재발하지 않도록 `tests/contract/test_public_track_c_gate.py`를 추가했습니다. 기존 signup flag 테스트와 같은 방식으로 Dockerfile `ARG`/`ENV`, build arg 전달, 실행 셸 값 미상속, 환경파일의 명시적 boolean 요구, 예시 환경의 기본 차단, Frontend gate 표현식 존재와 `/dev/track-c/` 경로 부재를 확인합니다.

참고로 `tests/contract/test_deployment_remote_shell.py::test_remote_stdin_and_profile_gate[valid|zero]` 2건은 이 브랜치를 stash하고 `origin/develop` 그대로 돌려도 동일하게 재현됩니다. 이 PR과 무관한 기존 항목으로 보이는데, CI에서도 같은 상태인지 확인 부탁드립니다.

## 2. build 산출물 회귀 테스트 (WATCH)

말씀하신 대로 route 미등록 단위 테스트만으로는 gate 표현식이 상수로 접히지 않게 되는 회귀(예: 공유 모듈로 추출해 모듈 경계를 넘기는 변경)를 잡지 못합니다. `frontend/scripts/verify-track-c-gate.mjs`를 추가하고 CI `frontend` job에 연결했습니다.

양방향으로 확인합니다.

| flag | 확인 내용 |
| --- | --- |
| `false` | `TrackCPage-*` chunk 부재, `track-c`·`barrier-responses`·`support-action-plans`·"복약 안전 확인"·"이유와 도움 찾기" 문자열 부재 |
| `true` | `TrackCPage-*.js` chunk 존재 (gate가 항상 닫혀 있는 반대 방향 회귀 방지) |

`AppRouter.tsx`의 gate를 `const TRACK_C_PUBLIC = true`로 바꿔 일부러 깨뜨려 보면 아래처럼 잡힙니다.

```
PUBLIC_TRACK_C gate 검증 실패
  - VITE_PUBLIC_TRACK_C=false 산출물에 "TrackCPage chunk"가 남아 있습니다: TrackCPage-CqLu4TRq.js
  - VITE_PUBLIC_TRACK_C=false 산출물에 "barrier-responses"가 남아 있습니다: TrackCPage-CqLu4TRq.js
  ...
```

### 한 가지 공유드릴 점

검증 대상에서 `support_medication`은 뺐습니다. `SchedulePage`가 이 query parameter 이름을 문자열 리터럴로 들고 있어 flag가 닫혀도 번들에 남습니다. 다만 gate가 닫히면 `searchParams.get`의 반환값을 받는 binding이 상수 접기로 사라져 조회 결과가 어디에도 쓰이지 않습니다. 확인한 minify 결과입니다.

```js
let t=st(),[n,r]=On();n.get(`support_medication`);let i=n.get(`date`)
```

`n.get('support_medication')` 호출은 남지만 반환값을 받는 변수가 없습니다. 이름까지 지우려면 `setSearchParams` 호출부 등 Track C와 무관한 `SchedulePage` 코드를 재구성해야 해서 이번 범위에서는 제외하고 스크립트에 근거를 주석으로 남겼습니다. 이 잔존이 공개 게이트 관점에서 문제가 된다면 별도로 정리하겠습니다.

## 로컬 검증 결과

- `pnpm test` 825개 통과
- `pnpm lint` 통과
- `pnpm run verify:track-c-gate` 통과
- `pytest tests/contract tests/services` — 위에 적은 기존 2건과 docker 미설치 ERROR 7건 외 전부 통과

새 HEAD의 전체 CI green 확인 후 재리뷰 부탁드립니다.
